---
deployment:
  tasks:
    - export DEPLOYPATH=/home/fivedata/public_html/residual_visual
    - /bin/cp -R public_html $DEPLOYPATH
    - /usr/bin/find $DEPLOYPATH -type d -exec /bin/chmod 755 {} \;
    - /usr/bin/find $DEPLOYPATH -type f -exec /bin/chmod 644 {} \;